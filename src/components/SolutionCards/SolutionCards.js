import React from "react";

const TokenCards = ({title, text, link, classname}) => {
  return (
    <div>
      <div>
        <h2>Our Tokens</h2>
      </div>
    </div>
  );
};

export default TokenCards;
